<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.yandex.mapkit.mapview.MapView
        android:id="@+id/mapview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/imageButtonNearMe"
        android:layout_width="@dimen/d_50"
        android:layout_height="@dimen/d_50"
        android:layout_margin="@dimen/d_20"
        android:background="@drawable/bg_oval_grey"
        android:backgroundTint="@color/backgroundLoaderColor"
        android:contentDescription="@string/geolocation_text"
        android:src="@drawable/ic_24_near_me"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:tint="@color/elementPrimaryColor" />

</androidx.constraintlayout.widget.ConstraintLayout>